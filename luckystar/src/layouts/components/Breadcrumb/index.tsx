import React from "react";
import { Breadcrumb, Space } from "@arco-design/web-react";
import { IconPlus, IconSend } from "@arco-design/web-react/icon";

const BreadcrumbComponent: React.FC = (props) => {
  const { routes } = props;
  return <div className="font-zhuhai text-2xl">计算机二级考试</div>;
};

export default BreadcrumbComponent;
